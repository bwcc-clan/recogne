meta {
  name: Test
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/test-composite?arg=test
  body: none
  auth: none
}

params:query {
  arg: test
}

tests {
  test("status must be 200", function() {
    expect(res.status).to.eql(201);
  });
}
